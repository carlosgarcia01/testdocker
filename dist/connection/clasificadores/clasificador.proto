syntax = "proto3";
package clasificadorPackage;

service Clasificador {
  rpc clasificador(input) returns (response);
}

message input {
  string text = 1;
}
message response {
  string data = 1;
  int32 code = 2;
}
